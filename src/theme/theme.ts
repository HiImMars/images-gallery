import { createTheme, ThemeOptions } from "@mui/material/styles";

export const theme: ThemeOptions = createTheme({
  palette: {
    mode: "dark",
    primary: {
      main: "#673ab7",
    },
    secondary: {
      main: "#e91e63",
    },
  },
});
